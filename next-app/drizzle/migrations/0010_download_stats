-- View + download counters, shown publicly on every download card.
ALTER TABLE download_files ADD COLUMN view_count INTEGER DEFAULT 0;
ALTER TABLE download_files ADD COLUMN download_count INTEGER DEFAULT 0;
